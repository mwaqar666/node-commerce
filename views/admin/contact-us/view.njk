@extends('admin.layout.master')

@section('title', $title)

@section('parentPageTitle', $parentPageTitle)

@section('content')
    <div class="row clearfix">
        <div class="col-lg-12">
            <div class="card">
                <div class="header">
                    <h2>
                        <strong>{{ $parentPageTitle }}</strong> {{ $title }}
                    </h2>
                </div>
                <div class="body">
                    <div>
                        <h5>Name</h5>
                        <p>{{ $customerInquiry->name }}</p>
                    </div>
                    <div>
                        <h5>E-Mail</h5>
                        <p>{{ $customerInquiry->email }}</p>
                    </div>
                    <div>
                        <h5>Phone</h5>
                        <p>{{ $customerInquiry->phone }}</p>
                    </div>
                    <div>
                        <h5>Message</h5>
                        <p>{{ $customerInquiry->message }}</p>
                    </div>
                    <div>
                        <h5>Inquiry Date</h5>
                        <p>{{ $customerInquiry->created_at->toDateString() }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
@stop