@extends('admin.layout.master')

@section('title', $title)

@section('parentPageTitle', $parentPageTitle)

@section('content')
    <div class="row clearfix">
        <div class="col-lg-12">
            <div class="card">
                <div class="header">
                    <h2>
                        <strong>{{ $parentPageTitle }}</strong> {{ $title }}
                    </h2>
                </div>
                <div class="body">
                    <div class="row">
                        <div class="col-xl-3 col-lg-4 col-md-12">
                            <div class="preview preview-pic tab-content">
                                <div class="tab-pane active" id="primary-image">
                                    <img src="{{ asset($product->image) }}" class="img-fluid" alt>
                                </div>
                                @foreach ($product->multipleImages as $image)
                                    <div class="tab-pane" id="product-{{ $image->id }}">
                                        <img src="{{ asset($image->image) }}" class="img-fluid" alt>
                                    </div>
                                @endforeach
                            </div>
                            <ul class="preview thumbnail nav nav-tabs">
                                <li class="nav-item">
                                    <a class="nav-link active" data-toggle="tab" href="#primary-image">
                                        <img src="{{ asset($product->image) }}" alt>
                                    </a>
                                </li>
                                @foreach ($product->multipleImages as $image)
                                    <li class="nav-item">
                                        <a class="nav-link" data-toggle="tab" href="#product-{{ $image->id }}">
                                            <img src="{{ asset($image->image) }}" alt>
                                        </a>
                                    </li>
                                @endforeach
                            </ul>
                        </div>
                        <div class="col-xl-9 col-lg-8 col-md-12">
                            <div class="product details">
                                <h3 class="product-title mb-0">{{ $product->name }}</h3>
                                <p class="price mt-0">Current Price:
                                    <span class="col-amber" @if ($product->discount_price) style="text-decoration: line-through" @endif>${{ $product->regular_price }}</span>
                                    @if ($product->discount_price)
                                        <span class="col-amber">${{ $product->discount_price }}</span>
                                    @endif
                                </p>
                                <hr>
                                <p class="product-description">{!! $product->description !!}</p>
                                <table class="table table-bordered" id="product-attributes" style="color: black">
                                    <tbody>
                                        <tr>
                                            <td>Weight</td>
                                            <td>{{ $product->weight }} lbs</td>
                                        </tr>
                                        <tr>
                                            <td>Dimensions</td>
                                            <td>{{ $product->length }} x {{ $product->width }} x {{ $product->height }} in.</td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 col-sm-6">
            <div class="card w_data_1">
                <div class="body">
                    <div class="w_icon indigo">
                        <i class="zmdi zmdi-store"></i>
                    </div>
                    <h4 class="mt-3">Quantity</h4>
                    <span class="text-muted">{{ $product->quantity }}</span>
                    <div class="w_description text-{{ $product->quantity ? 'success' : 'danger' }}">
                        <i class="zmdi zmdi-{{ $product->quantity ? 'check' : 'close' }}"></i>
                        <span>{{ $product->quantity ? 'In' : 'Out Of' }} Stock</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 col-sm-6">
            <div class="card w_data_1">
                <div class="body">
                    <div class="w_icon pink">
                        <i class="zmdi zmdi-star-circle"></i>
                    </div>
                    <h4 class="mt-3">Feature Status</h4>
                    <span class="text-muted">­</span>
                    <div class="w_description text-{{ $product->featured ? 'success' : 'danger' }}">
                        <i class="zmdi zmdi-trending-{{ $product->featured ? 'up' : 'down' }}"></i>
                        <span>{{ $product->featured ? 'Featured' : 'Unfeatured' }}</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 col-sm-6">
            <div class="card w_data_1">
                <div class="body">
                    <div class="w_icon orange">
                        <i class="zmdi zmdi-shopping-cart"></i>
                    </div>
                    <h4 class="mt-3">Sale Status</h4>
                    <span class="text-muted">­</span>
                    <div class="w_description text-{{ $product->sale ? 'success' : 'danger' }}">
                        <i class="zmdi zmdi-shopping-cart{{ $product->sale ? '-plus' : '' }}"></i>
                        <span>{{ $product->sale ? 'In Sale' : 'Not On Sale' }}</span>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-3 col-md-6 col-sm-6">
            <div class="card w_data_1">
                <div class="body">
                    <div class="w_icon green">
                        <i class="zmdi zmdi-stop"></i>
                    </div>
                    <h4 class="mt-3">Product Status</h4>
                    <span class="text-muted">­</span>
                    <div class="w_description text-{{ $product->status ? 'success' : 'danger' }}">
                        <i class="zmdi zmdi-layers{{ $product->status ? '' : '-off' }}"></i>
                        <span>{{ $product->status ? 'Active' : 'Deactive' }}</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
@stop