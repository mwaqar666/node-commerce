@extends('admin.layout.master')

@section('title', $title)

@section('parentPageTitle', $parentPageTitle)

@section('page-style')
    <link rel="stylesheet" href="{{ asset('assets/plugins/jquery-datatable/dataTables.bootstrap4.min.css') }}"/>
    <link rel="stylesheet" href="{{ asset('assets/plugins/sweetalert/sweetalert.css') }}"/>
@stop

@section('content')
    <div class="row clearfix">
        <div class="col-lg-12">
            <div class="card">
                <div class="header">
                    <h2>
                        <strong>{{ $parentPageTitle }}</strong> {{ $title }}
                    </h2>
                    <ul class="header-dropdown">
                        <li class="dropdown">
                            <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <i class="zmdi zmdi-more"></i>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-right slideUp">
                                <li>
                                    <a href="{{ route('admin.product.create') }}">Create</a>
                                </li>
                                <li>
                                    <a href="javascript:void(0)">Another action</a>
                                </li>
                                <li>
                                    <a href="javascript:void(0)">Something else</a>
                                </li>
                            </ul>
                        </li>
                        <li class="remove">
                            <a role="button" class="boxs-close">
                                <i class="zmdi zmdi-close"></i>
                            </a>
                        </li>
                    </ul>
                </div>
                <div class="body">
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped dataTable js-exportable" style="color: black">
                            <thead>
                            <tr>
                                <th>#</th>
                                <th>Product ID</th>
                                <th>Name</th>
                                <th>Regular Price</th>
                                <th>Discount Price</th>
                                <th>Quantity</th>
                                <th>Image</th>
                                <th>Featured</th>
                                <th>Sale</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                            </thead>
                            <tbody>
                            @foreach ($products as $index => $product)
                                <tr data-record-id="{{ $product->id }}">
                                    <td>{{ $index + 1 }}</td>
                                    <td>{{ $product->id }}</td>
                                    <td>{{ $product->name }}</td>
                                    <td>$ {{ $product->regular_price }}</td>
                                    <td>$ {{ $product->discount_price }}</td>
                                    <td>{{ $product->quantity }}</td>
                                    <td>
                                        <img src="{{ asset($product->image) }}" alt="{{ $product->name }}">
                                    </td>
                                    <td>@if ($product->featured) Featured @endif</td>
                                    <td>@if ($product->sale) Sale @endif</td>
                                    <td>@if ($product->status) Active @else Deactive @endif</td>
                                    <td>
                                        @can('accessTo', ['view', 'Products'])
                                            <a href="{{ route('admin.product.view', $product->id) }}" class="btn btn-success btn-icon btn-icon-mini btn-round" data-view>
                                                <i class="zmdi zmdi-eye"></i>
                                            </a>
                                        @endcan
                                        @can('accessTo', ['update', 'Products'])
                                            <a href="{{ route('admin.product.edit', $product->id) }}" class="btn btn-primary btn-icon btn-icon-mini btn-round" data-edit>
                                                <i class="zmdi zmdi-edit"></i>
                                            </a>
                                        @endcan
                                        @can('accessTo', ['delete', 'Products'])
                                            <a href="javascript:void(0)" class="btn btn-danger btn-icon btn-icon-mini btn-round" onclick="deleteRecord({{ $product->id }}, '{{ route('admin.product.delete', $product->id) }}')">
                                                <i class="zmdi zmdi-delete"></i>
                                            </a>
                                        @endcan
                                    </td>
                                </tr>
                            @endforeach
                            </tbody>
                            <tfoot>
                            <tr>
                                <th>#</th>
                                <th>Product ID</th>
                                <th>Name</th>
                                <th>Regular Price</th>
                                <th>Discount Price</th>
                                <th>Quantity</th>
                                <th>Image</th>
                                <th>Featured</th>
                                <th>Sale</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
@stop

@section('page-script')
    <script src="{{ asset('assets/bundles/datatablescripts.bundle.js') }}"></script>
    <script src="{{ asset('assets/plugins/jquery-datatable/buttons/dataTables.buttons.min.js') }}"></script>
    <script src="{{ asset('assets/plugins/jquery-datatable/buttons/buttons.bootstrap4.min.js') }}"></script>
    <script src="{{ asset('assets/plugins/jquery-datatable/buttons/buttons.colVis.min.js') }}"></script>
    <script src="{{ asset('assets/plugins/jquery-datatable/buttons/buttons.flash.min.js') }}"></script>
    <script src="{{ asset('assets/plugins/jquery-datatable/buttons/buttons.html5.min.js') }}"></script>
    <script src="{{ asset('assets/plugins/jquery-datatable/buttons/buttons.print.min.js') }}"></script>
    <script src="{{ asset('assets/plugins/jquery-datatable/buttons/buttons.pdf.min.js') }}"></script>
    <script src="{{ asset('assets/plugins/sweetalert/sweetalert.min.js') }}"></script>
    <script src="{{ asset('assets/plugins/axios/axios.min.js') }}"></script>
    <script src="{{ asset('assets/js/pages/tables/jquery-datatable.js') }}"></script>

    @if (session()->has('flashMessage'))
        <script>
            success('{{ session('flashMessage') }}');
        </script>
    @endif

@stop