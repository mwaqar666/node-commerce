<aside id="leftsidebar" class="sidebar">
    <div class="navbar-brand">
        <button class="btn-menu ls-toggle-btn" type="button">
            <i class="zmdi zmdi-menu"></i>
        </button>
        <a href="{{ route('admin.dashboard.index') }}">
            <img src="{{ asset('images/logo.png') }}" width="25" alt="Evoke - The Culture">
            <span class="m-l-10" style="font-size: 15px">{{ config('app.name') }}</span>
        </a>
    </div>
    <div class="menu">
        <ul class="list">
            <li>
                <div class="user-info">
                    <div class="image">
                        <a href="javascript:void(0)">
                            <img src="{{ asset('assets/images/profile_av.jpg') }}" alt="User">
                        </a>
                    </div>
                    <div class="detail">
                        <h4>{{ auth()->user()->full_name }}</h4>
                    </div>
                </div>
            </li>

            <!-- Dashboard -->
            <li class="{{ explode('.', $currentRouteName)[1] === 'dashboard' ? 'active open' : null }}">
                <a href="{{ route('admin.dashboard.index') }}">
                    <i class="zmdi zmdi-home"></i>
                    <span>Dashboard</span>
                </a>
            </li>

            <!-- Products -->
            @can('accessTo', ['create|view|update|delete', 'Products', false])
                <li class="{{ explode('.', $currentRouteName)[1] === 'product' ? 'active open' : null }}">
                    <a href="javascript:void(0)" class="menu-toggle">
                        <i class="zmdi zmdi-hc-fw"></i>
                        <span>Products</span>
                    </a>
                    <ul class="ml-menu">
                        @can('accessTo', ['view', 'Products'])
                            <li class="{{ in_array(explode('.', $currentRouteName)[2], ['list', 'view'], true) ? 'active' : null }}">
                                <a href="{{ route('admin.product.list') }}">List</a>
                            </li>
                        @endcan
                        @can('accessTo', ['create', 'Products'])
                            <li class="{{ explode('.', $currentRouteName)[2] === 'create' ? 'active' : null }}">
                                <a href="{{ route('admin.product.create') }}">Create</a>
                            </li>
                        @endcan
                    </ul>
                </li>
            @endcan

            <!-- Orders -->
            @can('accessTo', ['view|delete', 'Orders', false])
                <li class="{{ explode('.', $currentRouteName)[1] === 'order' ? 'active open' : null }}">
                    <a href="javascript:void(0)" class="menu-toggle">
                        <i class="zmdi zmdi-globe"></i>
                        <span>Orders</span>
                    </a>
                    <ul class="ml-menu">
                        @can('accessTo', ['view', 'Orders'])
                            <li class="{{ in_array(explode('.', $currentRouteName)[2], ['list', 'view'], true) ? 'active' : null }}">
                                <a href="{{ route('admin.order.list') }}">List</a>
                            </li>
                        @endcan
                    </ul>
                </li>
            @endcan

            <!-- Customer Inquiries -->
            @can('accessTo', ['view|delete', 'Contact Us', false])
                <li class="{{ explode('.', $currentRouteName)[1] === 'contact-us' ? 'active open' : null }}">
                    <a href="javascript:void(0)" class="menu-toggle">
                        <i class="zmdi zmdi-email"></i>
                        <span>Contact Us</span>
                    </a>
                    <ul class="ml-menu">
                        @can('accessTo', ['view', 'Contact Us'])
                            <li class="{{ in_array(explode('.', $currentRouteName)[2], ['list', 'view'], true) ? 'active' : null }}">
                                <a href="{{ route('admin.contact-us.list') }}">List</a>
                            </li>
                        @endcan
                    </ul>
                </li>
            @endcan

            <!-- Terms & Conditions -->
            @can('accessTo', ['create|view|update', 'Terms & Conditions', false])
                <li class="{{ explode('.', $currentRouteName)[2] === 'terms-and-conditions' ? 'active open' : null }}">
                    <a href="{{ route('admin.policy.terms-and-conditions.view') }}">
                        <i class="zmdi zmdi-shield-security"></i>
                        <span>Terms & Conditions</span>
                    </a>
                </li>
            @endcan

            <!-- Privacy Policy -->
            @can('accessTo', ['create|view|update', 'Privacy Policy', false])
                <li class="{{ explode('.', $currentRouteName)[2] === 'privacy-policy' ? 'active open' : null }}">
                    <a href="{{ route('admin.policy.privacy-policy.view') }}">
                        <i class="zmdi zmdi-shield-security"></i>
                        <span>Privacy Policy</span>
                    </a>
                </li>
            @endcan

            <!-- Cookies Policy -->
            @can('accessTo', ['create|view|update', 'Cookies Policy', false])
                <li class="{{ explode('.', $currentRouteName)[2] === 'cookies-policy' ? 'active open' : null }}">
                    <a href="{{ route('admin.policy.cookies-policy.view') }}">
                        <i class="zmdi zmdi-shield-security"></i>
                        <span>Cookies Policy</span>
                    </a>
                </li>
            @endcan

            <!-- Roles -->
            @can('accessTo', ['create|view|update|delete', 'Role', false])
                <li class="{{ explode('.', $currentRouteName)[1] === 'role' ? 'active open' : null }}">
                    <a href="javascript:void(0)" class="menu-toggle">
                        <i class="zmdi zmdi-lock-outline"></i>
                        <span>Roles</span>
                    </a>
                    <ul class="ml-menu">
                        @can('accessTo', ['view', 'Role'])
                            <li class="{{ in_array(explode('.', $currentRouteName)[2], ['list', 'view'], true) ? 'active' : null }}">
                                <a href="{{ route('admin.role.list') }}">List</a>
                            </li>
                        @endcan
                        @can('accessTo', ['create', 'Role'])
                            <li class="{{ explode('.', $currentRouteName)[2] === 'create' ? 'active' : null }}">
                                <a href="{{ route('admin.role.create') }}">Create</a>
                            </li>
                        @endcan
                    </ul>
                </li>
            @endcan

            <!-- Users -->
            @can('accessTo', ['view|update|delete', 'Users', false])
                <li class="{{ explode('.', $currentRouteName)[1] === 'user' ? 'active open' : null }}">
                    <a href="javascript:void(0)" class="menu-toggle">
                        <i class="zmdi zmdi-account-o"></i>
                        <span>Users</span>
                    </a>
                    <ul class="ml-menu">
                        @can('accessTo', ['view', 'Users'])
                            <li class="{{ in_array(explode('.', $currentRouteName)[2], ['list', 'view'], true) ? 'active' : null }}">
                                <a href="{{ route('admin.user.list') }}">List</a>
                            </li>
                        @endcan
                    </ul>
                </li>
            @endcan
        </ul>
    </div>
</aside>
